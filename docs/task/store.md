To create a store, we have three steps:
1. Add a zone.
2. Add a city.
3. We can add the details of this store and create the store.
Also, whenever we are using the drop-down, we have to use a custom library for a drop-down or something like that, because we have to give a search option also in that drop-down.
Make the add store page look modern, fancy, and creative, with better and simpler UI/UX that users will like. Make it user-friendly with modern, fancy UI and micro interactions and small animations. Make it three steps:
1. Each step can be filled.
2. In the third step, we can see the zone and city with a disabled input, and the user can add the rest details to create a store.

Also, while creating a store, we do not require a company input field to show who the logged-in user is creating the store. It is assigned with that company only behind the scenes. The created stores are only visible to that user, not everyone. The admin can see each and every user's stores, companies, etc. We will do this later, but for now remove the company input field from the user store, and it will be by default assigned while we are creating.