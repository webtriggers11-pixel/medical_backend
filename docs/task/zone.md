Make zone a separate entity. Create a separate table for zone. It is not linked to any one. While creating a city, we have to select zone first, then we want the relation like this I want


 Companies === client so use client text everywhere in frontend , user ==== client ok so dont confused user login is ====client login